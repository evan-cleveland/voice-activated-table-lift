
Private library directory for the ESP32 actuator firmware.

Current status:

- No project-specific libraries are defined.
- All logic lives in `src/main.cpp`.

Use this folder if you later extract reusable modules such as:

- motor driver abstractions
- command decoding helpers
- safety or limit-switch handling

PlatformIO will automatically build libraries placed under `lib/<name>/`.
